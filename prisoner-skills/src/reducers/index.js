const initialState = {
  isLoading: false,
  error: '',
  prison_name: '',
  number_of_prisoners: '',
  prison_address: '',
  inmates: []
}

export const reducer = (state = initialState, action) => {
  switch(action.type){

    default:
      return state
  }
}